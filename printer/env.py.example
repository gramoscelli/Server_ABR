# Printer Service Configuration Template
# Copy this file to env.py and configure for your environment

# API Configuration
APP_HOST = "http://admin.abr.net:3000"  # Server URL
API_KEY = ""  # Your API key from /api/api-keys (required for authentication)

# Windows Printer Configuration (for tirada.py)
WINDOWS_PRINTER_NAME = "Microsoft Print to PDF"  # Change to your printer name

# ESC/POS Network Printer Configuration (for recibo_*.py)
ESCPOS_NETWORK_HOST = "192.168.1.100"  # Printer IP address
ESCPOS_NETWORK_PORT = 9100             # Standard ESC/POS port

# ESC/POS Serial Printer Configuration
ESCPOS_SERIAL_PORT = "COM1"  # Windows: COM1, COM2, etc. Linux: /dev/ttyUSB0, /dev/ttyS0
ESCPOS_SERIAL_BAUDRATE = 9600
ESCPOS_SERIAL_BYTESIZE = 8
ESCPOS_SERIAL_PARITY = "N"  # N=None, E=Even, O=Odd
ESCPOS_SERIAL_STOPBITS = 1

# Print Settings
PAPER_WIDTH_MM = 80  # Thermal printer paper width in mm
CHARACTERS_PER_LINE = 48  # Characters per line for thermal printer
DPI = 203  # Dots per inch (thermal printer resolution)

# Tirada (Fee Collection Report) Settings
TIRADA_TITLE = "RECIBO DE COBRO"
TIRADA_ORGANIZATION = "Asociación Bernardino Rivadavia"
TIRADA_RECORDS_PER_PAGE = 8  # Records per printed page

# Debug Settings
DEBUG = False
VERBOSE_LOGGING = False
